{% extends 'base.html.twig' %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1>Room</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ room.name }}</td>
            </tr>
            <tr>
                <th>Capacité</th>
                <td>{{ room.capacity }}</td>
            </tr>
            <tr>
                <th>Images</th>
                <td>
                {% for image in room.images %}
                    <img src="/assets/images/{{ image.path }}" width="40" alt="">
                    {% endfor %}
                </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_room_index') }}" class="btn bg-info text-white mb-2 mt-2">Retour</a>

    <a href="{{ path('app_room_edit', {'id': room.id}) }}" class="btn bg-warning text-white mb-2 mt-2">Modifier</a>

    {{ include('room/_delete_form.html.twig') }}
{% endblock %}
